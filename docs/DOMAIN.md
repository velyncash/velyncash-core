# Domain Overview

Core domains:
- Auth (username/password + PIN)
- Wallet (SOL/USDC balances, transfers, identity)
- Payments (QR requests, merchant pay, conversion rules)
- Cards (virtual card lifecycle)
- Notifications (realtime activity updates)
